class FallbackDex:
    def __init__(self, config):
        self.config = config

    def activate(self):
        print("Fallback para DEX alternativo ativado.")
        # Lógica de fallback para outras DEX
